{
  "name": "yarn-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Test yarn monorepo for always-up-to-date CLI",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  },
  "devDependencies": {
    "typescript": "^4.8.4",
    "vitest": "^0.32.2"
  }
}
