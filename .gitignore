node_modules
.copilot
dist
*.backup
*.log
coverage
.DS_Store
.clinerules

# Ignore lock files in test repos to prevent Dependabot security alerts
# None of these are actually used in the build, they are just there for testing purposes.
testRepos/**/package-lock.json
testRepos/**/yarn.lock
testRepos/**/pnpm-lock.yaml
testRepos/**/bun.lock
